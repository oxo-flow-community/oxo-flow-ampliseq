# MultiQC env (upstream module pin). Channel NAMES only — CN users point
# conda at a domestic mirror, international users use the defaults.
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - multiqc=1.34
